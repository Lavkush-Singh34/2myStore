<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MyStore</title>
    <link href="/css/output.css" rel="stylesheet">
</head>
<body class="bg-gray-100">
    <nav class="bg-white shadow-md">
        <div class="container mx-auto px-4 py-3 flex justify-between items-center">
            <a href="/" class="text-2xl font-bold text-gray-800">MyStore</a>
            <div class="flex space-x-4">
                <a href="/" class="text-gray-700 hover:text-gray-900">Home</a>
                <a href="/products" class="text-gray-700 hover:text-gray-900">Products</a>
                <% if (!locals.user) { %>
                    <a href="/users/login" class="text-gray-700 hover:text-gray-900">Login</a>
                    <a href="/users/signup" class="bg-blue-500 text-white px-3 py-1 rounded hover:bg-blue-600">Sign Up</a>
                <% } else { %>
                    <a href="/users/profile" class="text-gray-700 hover:text-gray-900">Profile</a>
                    <form action="/users/logout" method="POST" class="inline">
                        <button type="submit" class="text-gray-700 hover:text-gray-900">Logout</button>
                    </form>
                <% } %>
            </div>
        </div>
    </nav>

    <main class="container mx-auto px-4 py-6">
        <%- body %>
    </main>

    <footer class="bg-white shadow-md mt-6 py-4 text-center">
        <p>&copy; 2024 MyStore. All rights reserved.</p>
    </footer>

    <script src="/js/main.js"></script>
</body>
</html>